# data/

Placeholder for locally regenerated derived metrics (gitignored; the
`data/derived/` convention is implemented by `derived_root()` in
[`src/core/paths.py`](../src/core/paths.py)). No data ships with this
release — see [`../docs/DATA.md`](../docs/DATA.md) for the public source and
licence of every external asset and the commands that regenerate each
experiment's inputs.
